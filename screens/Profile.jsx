import { NativeBaseProvider, Image, HStack, VStack, Badge, Box, Center, Avatar, Heading, Text } from "native-base";
import * as React from "react";

export default function Profile(props) {
    return (
	<NativeBaseProvider>
	</NativeBaseProvider>
    )
}
